import React from 'react'
import Layout from '../components/layout'

const NotFoundPage = () => (
  <Layout>
    <h1>Page not found!</h1>
    <p>We're sorry but we can't find this page!</p>
  </Layout>
)

export default NotFoundPage
